namespace ProyectoAsistencia.Core.Modelo
{
    // Representa los dos roles que puede tener un usuario en el sistema.
    // Corresponde a los registros de la tabla "roles" en la base de datos.
    public enum Rol
    {
        ADMINISTRADOR,
        EMPLEADO
    }
}
